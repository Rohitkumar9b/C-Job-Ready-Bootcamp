#include<stdio.h>
int main()
{
    int a,b,c;
    a=sizeof(int);
    b=sizeof(float);
    c=sizeof(double);
    printf("Size of int is %d,Size of float is %d,Size of double is %d",a,b,c);
    return 0;
}
