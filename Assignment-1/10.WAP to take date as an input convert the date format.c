#include<stdio.h>
#include<string.h>
int main()
{
    int d,D,m,M,Y,y,i;
    char s[10];
    printf("Enter date in DD/MM/YYYY\n");
    puts(s);
    for(i=0;s[i];i++)
    {
        if(s[i]>=48 && s[i]<=57)
        {
            s[i]
        }
    }
    printf("Day – %d ,Month – %d , Year – %d",d,m,y);
    return 0;
}

